apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: isabelapires-shared-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
  volumeName: project2-pv-isabelapires